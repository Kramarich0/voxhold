pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "**/*.{js,jsx,ts,tsx}" 
      run: bun lint:fix {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commit-check:
      run: bun scripts/check-commit-name.ts {1}

pre-push:
  parallel: true
  jobs:
    - name: type-check
      run: bun run type-check

    - name: tests
      run: bun test:run

post-merge:
  commands:
    install:
      run: bun install
